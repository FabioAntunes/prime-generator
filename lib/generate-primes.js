function generatePrimes(N = 1, done) {
  gridSize = Math.pow(N + 1, 2) - 1;
  const sieve = [];
  const primes = [];
  const maxLimit = Math.sqrt(Number.MAX_SAFE_INTEGER);

  done(null, primes);
}

module.exports = exports = generatePrimes;
